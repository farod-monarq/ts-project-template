import { Options } from "../../../types";

const environment: Options = {
	baseURL: 'https://develop.operator.egamingchain.io',
	timeout: 5000,
	headers: {
		'Content-Type': 'application/json',
	},
};

export default environment;